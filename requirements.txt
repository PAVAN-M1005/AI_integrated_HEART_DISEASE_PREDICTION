
Core dependencies for the Flask application

flask

Data manipulation and scientific computing

pandas
numpy

Machine Learning framework (Scikit-learn is often used implicitly with joblib)

While not strictly required if you only load the model, it's good practice

to include the version used for training. Assuming scikit-learn for the model.

scikit-learn

Model persistence (loading the trained model and scaler)

joblib

Database

Note: sqlite3 is built into standard Python, but if you were to use a more

complex database connection layer, you would list it here (e.g., SQLAlchemy, psycopg2)

Since you are using raw sqlite3, no external package is required.
